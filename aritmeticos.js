const [a, b, c ,d] = [3, 5, 1 ,15]

const soma = a + b + c + d
const subtracao = d - c
const multiplicacao = a *b
const divisao = d / a
const modulo = a % 2 // o modulo vai dividir o numero por dois. Se ele não sobrar, então e par. Se ele sobra é impar
 
console.log(soma, subtracao, multiplicacao, divisao, modulo)